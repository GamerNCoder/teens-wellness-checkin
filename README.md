# Teen Wellness Check-in

**Non-clinical** mood + journal in the browser (`localStorage`). Designed for **safety-first** messaging.

## Boundaries (important for essays & interviews)

- Not therapy, not diagnosis, not risk assessment.
- Includes **988** / **Crisis Text Line** / **SAMHSA** links.
- No server = fewer privacy risks; tradeoff: no cross-device sync until you add an explicit opt-in backend.

## Run

```bash
npm install && npm run dev
```

**PWA-ready** (`public/manifest.webmanifest`), touch-friendly controls. See **`MOBILE.md`** for Expo + AsyncStorage.

## v2 ideas

- Export encrypted backup file.
- School counselor “resource sheet” mode (static content only).

MIT — portfolio.
